fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
sqlalchemy==2.0.23

# ─── Phase 6: PostgreSQL / migration foundation ───────────────────────────────
# Schema migrations (canonical for PostgreSQL; SQLite uses legacy bootstrap)
alembic>=1.12
# PostgreSQL driver (only required when DATABASE_URL points at PostgreSQL)
psycopg[binary]>=3.1
pandas==2.1.3
numpy==1.26.2
scikit-learn==1.3.2
# (scikit-learn also powers the Phase 5 ML-assisted semantic layer:
#  services/semantic_service.py — TF-IDF concept bank, no new deps needed)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
aiofiles==23.2.1
openpyxl==3.1.2

# ─── Multilingual support ────────────────────────────────────────────────────
# Language detection (no API key required)
langdetect==1.0.9

# Translation (uses Google Translate via free API — no key for basic use)
# Swap for IndicTrans2 or another offline model in production
deep-translator==1.11.4

# ─── LLM Explanation Provider ───────────────────────────────────────────────
groq>=0.9.0
